

```sh
python -m venv textload_venv #create the venv | python3 if you are on linux

textload_venv\Scripts\activate #activate venv (windows)
source textload_venv/bin/activate #(linux)

pip install langchain==0.0.150 openai==0.27.0 faiss-cpu==1.7.2
```